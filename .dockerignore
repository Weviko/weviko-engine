# Ignore everything by default so the Docker build context stays small.
*

# Keep only the files needed to build and run the crawler image.
!Dockerfile
!.dockerignore
!main.py
!requirements.txt
!requirements-weviko-crawler.txt
